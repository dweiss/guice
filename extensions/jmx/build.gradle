description = 'Guice (Carrot Search fork) - Extensions - JMX'
ext.osgiModule = 'com.google.inject.tools.jmx'

// The only test is not actually a unit test.
tasks.withType(Test).configureEach {
  enabled = false
}
